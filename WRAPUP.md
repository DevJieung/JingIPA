# WRAPUP — 작업을 마칠 때

이 순서 그대로. 하나라도 깨졌으면 넘어가지 말고 보고할 것.
(rogame 의 같은 문서를 이 저장소에 맞게 옮겨 온 것이다.)

## 1. 검증

```bash
tools/verify.sh
```

`전부 통과` 가 나와야 한다. 급할 때는 `tools/verify.sh quick`(족보 전수 검사와
자동 플레이를 줄인다, 약 3분)을 쓰되, **마무리 전에는 반드시 전체를 한 번 돌린다.**

기대값:

| 검사 | 기대 |
|---|---|
| 0. 커맨드라인 | `core/ game/ 어디도 커맨드라인을 읽지 않음` |
| 0-1. 무늬 | `무늬는 전부 도형으로 그린다` (♠♦♣ 를 글자로 쓰면 안 된다 — 아래 참고) |
| 1. 캐릭터 표 | `core/roster.gd 가 tools/roster.json 과 일치` |
| 1-1. 효과음 | `소리 41개를 만들었습니다 … 1.1MB` (`tools/gen_sfx.py` 가 코드로 합성한다) |
| 2. 임포트 | `SCRIPT ERROR` · `Parse Error` 가 하나도 없음 |
| 3. 부팅 | 오류 없이 끝남 |
| 4. 표 검사 | `스크립트 32개 중 32개 파스됨` · `길 …px · 기본 속도로 …초` · `사거리 없음 확인 (…)` · `영웅 편성 정상 …` · `속성 정상 …` · `테마 50개 정상 …` · `100탄 편성 정상 …` · `영문 이름 50개 …` · `소리 39개 모두 있음` · `테마 그림 100장 모두 있음` · `그림 122장 모두 있음` · `판정: 정상` |
| 5. 족보 판정 | `전수 검사 2598960판` · **족보 열 가지 개수가 전부 이론값과 일치** · `판정: 정상` |
| 6. 화면 한 바퀴 | `판정: 정상` (타이틀 → **테마 판** → 카드 → **편성 판** → 전투 → **전과 판** → 상점을 실제로 눌러서 돈다. **자동 저장이 판을 담고 되돌리는지**도 여기서 본다) |
| 7. 자동 플레이 | `판정: 정상` · 12판이면 `클리어 1~6판`(기대 2판 안팎, 24판이면 3~6판) · 도달 중간값 **92~98탄** · **1~6탄은 크리스탈을 잃지 않는다** |
| 8. 폰트 | `화면에 쓰는 모든 문자가 폰트에 있습니다` |
| 9. APK | `ok: APK /home/…/pokerdefense-test.apk (약 85~90M)` — **홈 디렉터리에 굽는다**. 테마 그림 100장이 들어가면서 58M → 87M 이 됐다 |

⚠ **멈춤은 조용한 대기가 아니라 실패다.** `verify.sh` 의 모든 Godot 호출에는 `timeout`
이 걸려 있고, 넘기면 출력 파일에 `!! 멈춤` 을 적는다. 실제로 스크립트 하나가 파스가 안 되면
씬이 `quit()` 을 영영 못 불러서 3분을 그냥 기다린 적이 있다.

## 2. 화면을 고쳤으면 사진도 새로

```bash
python3 tools/screenshot.py                  # 기본 열일곱 장
python3 tools/screenshot.py reveal:9 battle:14 shop:9
python3 tools/screenshot.py battle:30 shopp:14 shopc:24 # 보스탄 · 패시브 탭 · 크리스탈 탭
python3 tools/screenshot.py battle:1 frost:12           # 1탄(체력 막대) · 얼음(둔화) 연출
python3 tools/screenshot.py team:7 swap:24              # 편성 판 (자리 남음 · 꽉 참)
python3 tools/screenshot.py theme:11 result:12          # 테마 판 · 라운드 전과 판
python3 tools/screenshot.py bullets:16 draw:6           # 속성별 탄 모양 · 도트 포커 판
python3 tools/screenshot.py stun:12 zone:24 zone1:40    # 마비 겉그림 · 장판(넷 · 하나)
python3 tools/screenshot.py shopp:14                    # 패시브 문양 26장이 카드에 얹힌 모습
python3 tools/screenshot.py dbg:14 dbgh:30 dbgm:24 dbgt:20   # 디버그 오버레이 네 탭(F3)
python3 tools/screenshot.py hall:24 title:39            # 굴러가는 전당 · 이어하기가 있는 타이틀
```

`build/shots/` 에 PNG 로 들어간다. **눈으로 한 번 봐라.** 무언가가 가려지거나 화면 밖으로
나가는 문제는 테스트로 안 잡힌다 — 실제로 이 도구가 두 개를 잡았다:

- 로열 연출에서 **빛살이 족보 이름을 덮어** 무슨 족보인지 안 보였다 (`Fx.draw_back` 이 생긴 이유)
- 성역에 영웅이 스물넷 서니 **크리스탈이 통째로 가려졌다** — 목숨이 몇 개 남았는지가
  안 보이는 디펜스 게임은 성립하지 않는다 (크리스탈을 배우들보다 나중에 그리게 된 이유)
- 영웅 설명 두 줄이 **화면 아래로 잘려** 나갔다
- 상점 탭이 다섯이 되자 **탭 줄이 오른쪽 설명 한 줄을 덮었다** (탭 폭을 170 → 140 으로)
- 패시브 카드를 252px 로 두었더니 아래 줄의 **사는 단추가 화면 밖(y 820)** 으로 나갔다 —
  진열을 보고도 살 수가 없었다 (216px 로 줄인 이유)
- 그림 카드(J·Q·K)의 판 속을 무늬 색 알파로 깔았더니, 그 위에 같은 색으로 그린 왕관과
  글자가 통째로 묻혀서 **검은 네모 한 덩이**로 찍혔다
- 테마 판에서 몸 분포 막대를 380px 로 두었더니 오른쪽 끝의 속성 표시가 **옆 칸을 파고들었다**
- **스페이드 자리에 하트 도트판이, 하트 자리에 스페이드가** 들어 있었다. 색은 무늬 번호로
  정하므로 화면에는 **검은 하트와 빨간 스페이드**가 찍혔다 — 사진으로만 잡히는 종류다
  (지금은 `ns_check` 가 모양으로도 잡는다)
- 「이어하기」가 생기자 단추가 둘이 되어 **아래 단추가 평생 기록 줄을 덮었다.** 저장된
  판이 없으면 그 갈래가 안 도므로, `title:39` 처럼 **판을 심어 두고** 찍어야 보인다
- 전당 창이 **한 줄**뿐이라 스물다섯 명을 다섯 판에 나눠 보게 돼 있었고, 줄에 안 맞는
  자리에서 손을 떼면 칸이 판 밖 120px 까지 나가 **「전투 시작」 위에서 눌렸다**
- 얼음 바닥 그림을 앞 파편과 같은 폭으로 뽑았더니 파편이 바닥을 통째로 덮어서,
  **그리고 있는데도 사진에 한 조각도 안 나왔다** (바닥은 앞보다 넓어야 한다)
- PNG 만 새로 뽑고 `--import` 를 안 돌렸더니 Godot 이 **옛 캐시**를 그대로 써서
  새 그림 자리에 회색 판때기가 찍혔다
- 테마 **바닥** 그림을 배경과 같은 프롬프트로 뽑았더니, 투기장 원판 안에 「먼 산과 하늘이
  있는 호수 사진」이 붙어 나왔다 — 바닥은 위에서 곧장 내려다본 땅 무늬여야 한다
  (`roster.json` 의 `floor_prompt` 를 따로 둔 이유). **한 장 뽑아 보고 나머지를 돌려라** —
  백 장을 다 뽑고 나서 알면 두 시간을 버린다
- 테마 판 배경을 0.42 로 덮었더니 **능선과 날씨가 통째로 묻혀서** 「어디인지」가 안 보였다
- 영웅 편성 판의 **빈 칸이 글자만 떠 있어** 자리가 있는 줄도 몰랐다
- 활 안쪽에 **흰 배경이 갇혀** 남아서 캐릭터에 흰 판때기가 붙어 있었다 (`trapped_white`)
- 얼음 기둥을 몬스터 **몸 한가운데**에 세웠더니 스프라이트에 통째로 가려져 한 조각도
  안 보였다 (발밑 고리 위로 옮긴 이유)
- 바깥 길 맨 위에 선 몬스터의 **체력 막대가 위쪽 정보띠에 가려졌다** (y 를 72 아래로
  안 내리는 이유)
- 성역 고리가 92 라 영웅이 **크리스탈 제단을 밟고 선 것처럼** 보였다 (108 로 밀었다)
- 속성 아이콘을 처음 뽑았을 때 얼음은 **눈송이가 아니라 흩날리는 흰 점**으로, 전기는
  **알파벳 「Z」 그 자체**로 나왔다 (프롬프트에 「shaped like a bold letter Z」라고 쓴
  값이다 — 모델은 적힌 말을 따른다, CLAUDE.md 4-4 와 같은 함정). **32x32 짜리 아이콘은
  한 판에 늘어놓고 눈으로 봐라** — 일곱 장이면 7분이라 다시 뽑는 값이 싸다
- 준 피해 막대의 자를 「이번 탄 전체 체력」으로 바꿨더니, 홈(빈 막대)이 줄 배경과 **같은
  어두운 색**이라 막대가 허공에 뜬 알약으로 보였다 — 영웅 하나의 막대는 길어야 홈의
  5분의 1이라, 홈이 안 보이면 「전체의 몇 할인가」가 통째로 사라진다

찍는 데 시간이 좀 걸린다(소프트웨어 OpenGL 이라 전투 한 장에 1~2분). 여러 장을 한 번에
넘기면 Godot 을 한 번만 띄우므로 훨씬 빠르다.

## 3. 그림을 고쳤으면 다시 굽기

```bash
python3 tools/gen_art.py --list                  # 목록만
python3 tools/gen_art.py                         # 없는 것만 (한 장 65초)
python3 tools/gen_art.py --only 화염용 --force     # 한 놈만 다시
python3 tools/gen_roster.py                      # roster.json 을 고쳤으면 표를 다시 찍는다
~/.local/bin/godot --headless --path . --import  # ★ 새 PNG 는 임포트해야 게임이 본다
```

- 캐릭터를 **더하거나 지우는 것은 `tools/roster.json` 한 곳**만 고치면 된다.
  `core/roster.gd` 는 자동 생성이라 손으로 고치지 마라 (`verify.sh` 1번이 잡는다).
- 122장(캐릭터 **50** · 몬스터 25 · UI 47) 전부 다시 만들면 GPU 로 **약 2시간 15분**이다.
  캐릭터와 몬스터만이면 75장에 **약 85분**(`--kind unit,monster --force`).
- ★ 여기에 **스프라이트**가 따로 붙는다 — `tools/sprite/run50.sh` 로 약 100분
  (원화 50장 55분 + 클립 100장 45분).
  ★ **모델은 한 번만 올린다**(3~4분). 그래서 `--kind unit` 과 `--kind monster` 를 따로
    부르지 말고 쉼표로 이어서 한 번에 불러라.
  UI 47장 중 **26장이 패시브 문양**(`art/ui/pi_<패시브id>.png` 96x96)이고
  **7장이 속성·몸 아이콘**(`art/ui/el_*.png` 32x32)이다 —
  `python3 tools/gen_art.py --kind ui --only pi_` (약 17분) ·
  `python3 tools/gen_art.py --kind ui --only el_` (약 7분) 으로 따로 뽑는다.
  백그라운드로 돌려라. ★ 테마 배경 100장은 **부르지 않으면 안 뽑는다**(`--kind theme`).
- `gen_art.py` 는 흰 배경 오려내기가 실패하면(배경이 안 지워지거나 그림이 통째로
  지워지면) 끝에 `!! 눈으로 봐야 할 것` 으로 알려 준다. **조용히 넘어가지 마라.**
- **「갇힌 배경 …% 를 지웠다」**도 같은 줄에 뜬다(`roster.json` 에 `"holes": true` 를
  적은 그림만 지운다). **흰 옷·수염·칼날을 배경으로 잘못 봤으면 몸에 구멍이 뚫린다.**
  검사로는 절대 안 잡히니 그 그림만이라도 사진으로 봐라.
- **「갇힌 배경으로 보이는 덩어리 …% 가 있다」**는 아직 안 지웠다는 뜻이다. 그림에 흰
  판때기가 붙어 있으면 `roster.json` 에 `"holes": true` 를 적고 `--force` 로 다시 뽑아라.

## 3-2. 정지 그림을 다시 뽑았으면 **스프라이트도 다시**

스프라이트 파이프라인(`tools/sprite/`)은 제 원화를 따로 뽑지만(같은 시드 · 같은 앵커 ·
자세만 「쏘기 전」), 몸통 묘사는 `roster.json` 하나를 본다. 그러니 프롬프트를 고쳤으면
**둘 다** 다시 만든다.

```bash
gpujob run pd-sprite tools/sprite/run50.sh      # ★ 원화→클립→시트→반입 (약 100분)
~/.local/bin/godot --headless --path . --import # 새 PNG 를 임포트
python3 tools/gen_roster.py                     # anim 플래그 · 총구(muz) · 뻗는 시간(wind)
```

몇 명만 다시 할 때는 도구마다 `--only <id,id>` 를 준다:

```bash
python3 tools/sprite/make_ref.py    --route krea --only a,b --force
python3 tools/sprite/refine.py      --route krea --only a,b --all
python3 tools/sprite/wan_i2v.py     --route krea --only a,b --force
python3 tools/sprite/sprite_post.py --route krea --only a,b
python3 tools/sprite/to_game.py     --route krea --only a,b
```

★ **클립이 없어도 게임은 돈다**(정지 그림 한 장으로). 그래서 한 명이 실패해도
  마흔아홉은 그대로 얹는다.

★ **마지막 `gen_roster.py` 를 빼먹지 마라.** 총구 자리(`muz`)와 팔을 뻗는 시간(`wind`)이
  `art/anim/<id>/anim.json` 에서 나온다 — 클립을 다시 짜 놓고 이걸 안 돌리면 총구 불꽃만
  손끝에 있고 탄은 옛 자리에서 나간다. (`ns_check` 의 「총구」 줄이 두 파일을 대조해 잡고,
  `verify.sh` 1번도 표가 낡았으면 잡는다)

★ **캐릭터 몇 명만 다시 뽑을 때는 `tools/reroll_art.py`.** 후보를 여러 장 뽑아
  `build/cand/<id>/` 에 쌓아 두고 눈으로 고른 뒤 얹는다 — `gen_art.py --force` 는 바로
  덮어써서, 마음에 안 들면 모델을 3~4분씩 다시 올려야 한다.

```bash
python3 tools/reroll_art.py ember_apprentice:0,1,2,3 frost_novice:0,1,2,3
python3 tools/reroll_art.py --pick ember_apprentice=t2,frost_novice=t0
python3 tools/anim/autorig.py ember_apprentice && python3 tools/anim/build_clips.py ember_apprentice
```

## 4. APK 굽기 (매번)

★ **APK 는 언제나 홈 디렉터리에 굽는다.** 저장소 안(`build/`)에 굽지 마라 —
폰으로 옮길 때마다 경로를 찾아 들어가야 하고, 지운 줄 알았던 옛 APK 가 남는다.
`tools/verify.sh` 9단계도 같은 자리에 굽는다.

★ **사용자 지정: 파일명은 `pokerdefense-test.apk`로 고정하고 매번 덮어쓴다.**
기능명(`newmap`, `readability` 등)·날짜·번호를 붙인 새 이름을 만들지 않는다.
새 빌드가 성공한 뒤에만 기존 파일을 교체하고, 답변에서도 이 고정 경로를 안내한다.

```bash
tools/build_apk.sh      # 임시 폴더에 굽고 서명·광고 플러그인 매니페스트를 확인한 뒤 교체한다
```

답변 끝에 내려받는 방법까지 적을 것:

```bash
scp dgxmaruta@<호스트>:~/pokerdefense-test.apk .
adb install -r pokerdefense-test.apk
```

- `cannot connect to daemon at tcp:5037` 은 폰이 안 붙어 있어서 나는 것이라 오류가 아니다.
  `[ DONE ] export` 와 `Signed` 가 보이면 성공.
- **APK 가 안 나오면 조용히 넘어가지 말고 왜 실패했는지 말할 것.**
- 광고 플러그인이 들어오면서 **Gradle 빌드**(`use_gradle_build=true`)가 됐다. Gradle 8 은
  JDK 17 이 필요해서 `tools/build_apk.sh` 가 `build/godot-config` 의 편집기 설정
  (`build/toolchains/jdk17` · `~/Android/SdkFlutter`)으로 굽는다 — 기본 편집기 설정(Java 8)으로
  맨 `--export-debug` 를 부르면 Gradle 이 「JVM 11 이상」이라며 거절한다. `tools/verify.sh`
  9단계도 그 대본을 부른다. 스토어용 **AAB 는 이 머신에서 안 된다.** iOS 는 Xcode
  프로젝트까지만 나온다(맥이 있어야 IPA 가 된다).

## 5. 커밋

```bash
git add -A && git commit -m "..."
```

⚠ **이 저장소에는 아직 원격이 없다.** rogame 의 `origin`(DevJieung/JingIPA)은 **다른
프로젝트**이고, 거기에 밀면 그 저장소를 덮어쓴다. **절대 그러지 마라.**
GitHub 에 올리려면 사람이 먼저 저장소를 하나 만들고 알려 줘야 한다:

```bash
git remote add origin https://github.com/<사용자>/<새-저장소>.git
git push -u origin main
```

자격증명은 이미 걸려 있다 — `~/.ghtoken`(600) 의 fine-grained 토큰을 전역 git 설정의
`credential.https://github.com.helper` 가 읽어 넘긴다. 새 저장소에도 권한을 줘야 한다
(**Contents RW** 는 필수, Actions 를 쓸 거면 **Workflows RW · Actions RW** 도).

- ⚠ **토큰을 화면에 찍지 마라.** git·curl 출력은 반드시 걸러라:
  `... 2>&1 | sed 's/github_pat_[A-Za-z0-9_]*/<토큰>/g'`
- ⚠ **토큰을 `curl -H "Authorization: Bearer $T"` 로 넘기지 마라.** 프로세스 인자에 올라가
  `ps` · `pgrep -af` 로 이 머신의 누구에게나 보인다. 반드시 stdin(`curl --config -`)으로.
- 이 셸에는 터미널이 없어(`tty` 없음) **대화형 로그인이 원천적으로 불가능**하다.
  파일 기반 헬퍼가 유일한 길이다.

---

## 절대 잊으면 안 되는 것

- **`♠ ♦ ♣` 를 글자로 그리지 마라.** 번들 폰트(DinoKR = Noto Sans CJK KR Bold)에 이 셋이
  **없다**(♥ 만 있다). 글자로 그리면 카드가 통째로 두부(□)가 되고, `.import` 의
  `allow_system_fallback` 때문에 **실기기에서는 OS 폰트가 메워 줘서 눈으로는 절대 안
  잡힌다.** 무늬는 `Look.draw_suit()` 이 도형으로 그린다. `tools/check_font.py` 와
  `verify.sh` 0-1번이 이걸 지킨다.
- **`core/roster.gd` 를 손으로 고치지 마라.** 원본은 `tools/roster.json` 이고
  `tools/gen_roster.py` 가 찍어 낸다. 그림 크기(h)도 `tools/gen_art.py` 의 표에서 나오므로
  그림과 게임이 항상 같은 값을 본다.
- **밸런스 숫자를 감으로 고치지 마라.** `core/balance.gd` 를 건드렸으면
  `tests/balance_check` 를 반드시 돌려라. 몇 개는 손으로 계산할 수 없다:
  - 길이 짧거나 몬스터가 빠르면 **한 대도 못 맞고** 지나간다. 길 길이
    (`path_len`)와 `PATH_SPEED` 는 한 몸이다 — 지금은 걸어 들어오는 데 29초다.
  - 장판(`aura`)의 피해 배수가 0.55 였을 때, **모두**를 때리는 탓에 혼자
    50배를 뽑아 판이 그냥 깨졌다. 지금은 0.10 이다.
  - 체력 곡선은 `9.0 × 1.1090^(w-1) × mid_ramp(w) × early_tough(w) × theme_hp(rank)` 다.
    지수 하나만 있던 시절에는 앞·중반이 통째로 얇아서 **몬스터가 입구를 벗어나기 전에**
    죽었고(사용자가 「50탄까지 하나도 못 벗어난다」고 한 그것), 그러다 어느 탄에서
    지수가 사람의 힘을 앞지르면 크리스탈 스무 개가 한꺼번에 날아갔다 — 쉬운 게임이
    아니라 **아무 일도 안 일어나다가 갑자기 끝나는** 게임이었다.
    `mid_ramp` 가 그 자리를 메운다(1탄 1.0 → 스무 탄 3.4 → 3.6 에 눕는다).
    그리고 지수를 1.181 → 1.1090 으로 낮춰 벽을 뒤로 밀었다.
    **한 탄을 못 막으면 그 탄에서 스무 개가 통째로 날아가므로** 클리어율이 지수에 아주
    민감하다 — 실측(12판): 1.1135 → 1판, 1.1105 → 0판, 1.1075 → 5판.
    **셋째 자리에서 몇 배로 뛴다.**
  - 사거리도 같은 문제의 절반이었다. 로열 저격수가 530 × 1.55(저격) × 2.52(업그레이드)
    로 2000px 을 넘어 **투기장 전체**를 덮었다. 그때는 곡선을 낮춰 막았지만, 사용자가
    **사거리 개념 자체를 없애기로** 정해서 지금은 모두가 어디든 때린다 —
    그만큼의 세기는 `HP_GROW` 가 혼자 받아 낸다. 그리고 걷는 속도를 150 → 172 로 올려
    한 바퀴가 29초에서 25초로 줄었다.
  - **상성 배수는 밸런스 손잡이가 아니다.** 저항 0.40 이든 0.70 이든 24판 중 13~14판으로
    거의 같다. 상성이 올려 놓은 실력만큼은 `kill_gold` 로 6% 덜어내 되돌렸다(7판, 29%).
  - **안전망에 속성을 붙이면 안 된다.** 「벼락 폭탄」(지금은 없어진 아이템)을 전기로
    두었더니 상성 배수를 아무리 만져도 24판 중 0판이었다 — 후반 방어의 안전망이
    돌골렘에게만 반토막 났기 때문이다. 지금 그 자리는 패시브 「수정 방벽」이고,
    그것도 속성을 안 탄다.
  - **무기와 아이템을 통째로 없앴다**(사용자가 정한 것). 무기는 제일 센 셋을 사면 끝이라
    고를 것이 없었고, 아이템은 값만 내면 그 탄을 사 버리는 안전망이었다. 대신 패시브를
    스물여섯으로 늘리고 **셋만** 들게 했다 — 무엇을 버리는가가 곧 빌드다.
  - **골드가 지렛대로는 가장 세다.** `kill_gold` 를 25% 올렸더니 클리어가 4/24 → 21/24 이
    됐다. 체력 곡선을 먼저 맞추고 골드는 마지막에 만져라.
  - 보스가 크리스탈을 **세 개** 부수는 것도 밸런스 장치다(다섯이었다가 줄였다 —
    되사기가 생긴 뒤로 다섯은 50탄에서 2800골드라 사실상 판의 끝이었다). 광역·장판은 몬스터가 많을수록
    세지는데 보스는 혼자라 그 몫을 못 받는다. 한 개만 부수면 "보스는 통과시키는" 것이
    최선의 수가 된다(실제로 마지막 탄이 그랬다).
- **`build/` 를 저장소에 넣지 마라** (`.gitignore` 에 있다). 촬영본·로그·APK 가 쌓인다.
  반대로 **`art/` 는 넣는다** — GPU 로 다시 만들 수 있지만 한 시간이 걸린다.
- **`tools/gen_art.py` 의 화풍 앵커(`DOT`)를 가볍게 바꾸지 마라.** 후보 셋을 실제로 뽑아
  보고 고른 것이고(`build/styletest/`), 바꾸면 102장을 전부 다시 구워야 한다.
- 헤드리스 stdout 은 **블록 버퍼링**이다. 죽이거나 멈추면 여태 찍은 게 통째로 날아가서
  "실행이 아예 안 된다"로 오해하게 된다. 검사를 돌릴 때는 항상 `stdbuf -oL` 을 붙여라.
- `pkill -f "..."` 로 프로세스를 죽일 때, **그 문자열이 지금 셸의 명령줄에도 들어 있으면
  셸 자신이 죽는다**(실제로 두 번 물렸다). PID 를 먼저 찾아 `kill` 하라.
