# 2026-09-11 화면·전투 연출 변경

- 영웅 획득 연출은 시간이 지나도 유지되며 터치하면 배치 화면으로 이동합니다.
- 족보가 높을수록 등장 섬광, 빛살, 충격파, 입자와 흔들림이 커집니다.
- 성역과 영웅 카드의 희귀도는 반 별부터 다섯 별로 표시하고 속성 아이콘을 함께 보여 줍니다.
- 새로 획득한 카드를 자동 선택하고 강조합니다. 같은 이름의 기존 영웅이 있어도 획득 라운드로 구별하며, 성역과 전당 사이를 옮겨도 강조가 따라갑니다.
- 오른쪽 전당은 불·얼음·물·전기·무상성 탭으로 구성합니다. 새 카드가 먼저, 그다음 희귀도 내림차순입니다. 한 페이지에 영웅 원화 4장을 표시합니다.
- 배치 화면의 획득 족보·캐릭터 배치 안내를 제거하고 다음 몬스터 헤더를 요청한 표기인 `Nest Stage`로 변경했습니다.
- 전투 하단의 타깃·드래그 안내와 입구 번호를 제거했습니다.
- 2배 피해에는 전용 충격파·빛살·픽셀 입자를 사용하고, 면역 피해는 큰 숫자 `0`으로 표시합니다.
- 50개 테마의 기존 바닥 원화를 연결하고, 다섯 환경의 새 바닥·길 소재를 함께 사용합니다. 각 10탄 블록의 실제 테마를 따릅니다.
- 광역 공격은 불·얼음·물·전기·무상성 각각 4프레임의 투명 픽셀 이미지로 표현합니다.
- 모든 라운드, 테마 등급, 일반 몬스터와 보스의 기준 체력을 기존 값의 1.20배로 조정했습니다.

## 생성 이미지

내장 `image_gen`을 사용했습니다. 생성 원본을 프로젝트에 복사했으며 게임은 아래 파일을 직접 사용합니다.

- `art/ui/el_elec.png`: 굵은 노란 번개 아이콘
- `art/terrain/biome_materials.png`: 2열(바닥·길), 5행(물·불·숲·바위·얼음)
- `art/effects/area_attacks.png`: 4열(시간 프레임), 5행(공격 속성)

최종 프롬프트 전체는 [생성 기록](../art/generated-assets.json)에 보존했습니다.

## 확인 결과

- `tests/presentation_check.tscn`: 328건 통과. 터치 전환, 실제 카드 선택·배치, 중복 카드 복원, 속성 탭, 정렬·페이지, 터치 영역, 체력 배수, 모든 테마 그림, 광역 수명·상한, 면역 0과 2배 연출을 검사했습니다.
- `tests/rules_check.tscn`: 1,523건 통과.
- `tests/flow_check.tscn`: 107건 통과. 비어 있는 구형 카드 더미 대신 현재 손패를 손상시키도록 검사 입력을 수정했습니다.
- `tests/monster_check.tscn -- --assets`: 88건 통과.
- `tests/play_check.tscn`: 타이틀 → 뽑기 → 터치 → 배치 → 전투 → 상점 흐름 통과. 획득 화면 터치를 추가하고 패배 저장 검사를 현재 부활 선택 규칙에 맞췄습니다.
- `tests/balance_check.tscn -- --runs 12`: 도달 중앙값 97탄, 최소 81탄, 100탄 클리어 2판. 1~6탄 크리스탈 손실 평균 0. 다섯 공격 속성의 사용 비율 14.4~24.8%.
- 실제 OpenGL 렌더링: 획득 연출 2종, 새 영웅 성역·전당 배치, 전당 페이지, 전투·광역 공격, 다섯 환경의 맵 확인. 스크린샷: `build/ui-refresh-shots/`.
- 폰트, 로스터, 효과음 파일 일치 및 `git diff --check` 통과.

기존 `tests/dmg_check.tscn -- --selftest`는 이전 중첩 배율 등 현재 제거된 규칙을 기대해 207건 실패했습니다. 이 구형 검사까지 포함한 전체 검증이 통과했다고 표시하지 않습니다. 현재 전투 규칙은 `rules_check`와 위 자동 플레이로 별도 확인했습니다.

APK 빌드·서명·광고 플러그인 매니페스트 검증과 새 이미지 포함 확인을 마쳤습니다. `/home/dgxmaruta/pokerdefense-test.apk`를 성공한 새 빌드로 교체했습니다(231,952,505바이트). 실제 안드로이드 기기 설치 테스트는 수행하지 않았습니다.
