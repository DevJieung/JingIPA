# APK 산출물

- 사용자 지정: APK는 항상 `/home/dgxmaruta/pokerdefense-test.apk`에 같은 이름으로 덮어쓴다.
- 기능명이나 날짜를 붙인 새 APK 이름을 만들지 않는다. 별도 요청이 없으면 번호도 붙이지 않는다.
- 빌드가 성공한 새 파일로만 기존 APK를 교체하고, 사용자에게 이 고정 경로를 안내한다.

# GitHub 모바일 빌드

- 사용자 지정: `DevJieung/JingIPA`의 `pokerdefense` 브랜치에 이 프로젝트를 올린다.
  기존 `main` 프로젝트는 보존한다. 일반 `git push`도 이 전용 브랜치를 향하게 한다.
- `.github/workflows/pokerdefense.yml`에서 매 푸시마다 APK와 IPA를 함께 만든다.
  기능 변경 후 APK만 만드는 흐름으로 회귀시키지 않는다.
- 워크플로 수정 시 수동 실행 등록용 `main`의 동일 파일도 동기화한다.
- 산출물과 서명 설정은 `docs/MOBILE_BUILDS.md`를 따른다. `.env`, 인증서,
  로컬 SDK/빌드 캐시와 생성용 대용량 원본을 Git에 넣지 않는다.

# 그래픽 디자인 전담 서브에이전트

- 사용자 지정: 이 프로젝트의 모든 스프라이트 및 UI 디자인 작업은
  `graphic_design_manager` 서브에이전트에 위임한다. 디자인 방향 결정, 에셋 제작·수정,
  UI의 시각 구현, 애니메이션·이펙트 표현, 실제 화면 시각 검수까지 해당 역할이 책임진다.
- 디자인이 포함된 요청에서는 메인 에이전트가 먼저 작업을 나누고 전담 서브에이전트를
  호출한다. 작은 디자인 수정도 같은 원칙을 따른다. 디자인에 영향이 없는 게임 로직,
  데이터 처리, 빌드 작업에는 디자인 서브에이전트를 불필요하게 호출하지 않는다.
- 역할 정의는 `.codex/agents/graphic_design_manager.toml`이다. 실행 도구에서 사용자 정의
  역할을 선택할 수 있으면 이 이름을 사용한다. 역할 선택 인자가 없는 환경에서는 해당
  파일을 읽고 `developer_instructions` 전문을 작업 지시와 함께 서브에이전트에 전달한다.
  작업 이름만 붙이고 역할 지침 전달을 생략하지 않는다.
- 같은 세션에서는 기존 디자인 담당에게 후속 작업을 전달한다. 새 세션에서는 역할 정의와
  `docs/DESIGN_SYSTEM.md`를 읽혀 재생성한다. 이전 에이전트의 기억이 자동으로 이어진다고
  가정하지 않는다. 디자인 담당 자신은 이 규칙을 이유로 같은 역할을 재귀 호출하지 않는다.
- 메인 에이전트는 사용자 요구·게임 규칙·연동 조건·완료 기준·수정할 파일을 전달하고,
  독립적인 게임 로직·연동 준비·검증 준비를 진행한다. 같은 파일은 동시에 수정하지 않는다.
  UI와 게임 로직이 섞인 파일은 담당 구간을 정하고 필요하면 순차로 수정한다.
- 메인 에이전트는 통합, 기능 회귀 검증과 APK 빌드를 책임진다. 통합 중 디자인 수정이
  필요하면 디자인 담당에게 돌려보내며, 임의로 스타일을 다시 바꾸지 않는다. 디자인 작업은
  담당자의 시각 검수 결과와 확인 자료를 받은 뒤 완료 처리한다. 사용자에게 매번 별도의
  디자인 승인을 요구하는 절차를 추가하지 않는다.
- 서브에이전트 도구가 제공되지 않거나 호출이 실패하면 위임이 실행되지 않았음을 알리고,
  독립적인 작업을 계속한다. 메인 에이전트가 작업한 것을 전담 에이전트의 결과라고 하지 않는다.
- 사용자 지시가 이 기본 분담을 명시적으로 바꾸면 그 지시를 따른다.
