# CLAUDE.md — 이 저장소에서 작업할 때

**포커 디펜스** — 트럼프 다섯 장으로 영웅을 뽑아 몬스터를 막는 디펜스. Godot 4.7.1, 안드로이드.

한 탄의 흐름은 이렇다:

1. **카드 다섯 장을 받는다.** 맘에 안 드는 카드는 **한 번 공짜로** 다시 뽑을 수 있고,
   이미 바꾼 카드는 **골드를 내야** 또 바꿀 수 있다 (15 → 30 → 60 … 두 배씩).
2. **결정하면 족보를 판정**하고, 그 등급의 캐릭터 중 **하나가 무작위로** 나온다.
   원페어면 원페어급 셋 중 하나, 로열이면 로열급 둘 중 하나.
2-1. **안뜰은 여섯 자리다.** 같은 캐릭터가 또 나오면 옆에 세우지 않고 **겹친다**
   (`3겹` = 공격력 3배). 처음 보는 캐릭터인데 자리가 없으면 **캐릭터 인벤토리**로 가고
   그 자리에서 **교체 창**이 뜬다. 상점의 「영웅」 탭(중간 정비)에서 언제든 다시 짠다.
3. **몬스터가 바깥 문으로 들어와 벽으로 나뉜 길을 돈다.** 길은 두 겹이고, 문은 90도씩
   어긋나 있어 나선으로 보인다. 영웅은 벽 안쪽 **안뜰**에 서서 벽 너머로 쏜다.
4. **길 끝에는 크리스탈 20개가 있다.** 한 마리가 닿을 때마다 하나가 깨진다(=목숨 -1).
   **보스는 다섯 개를 부순다.** 제한 시간은 없다 — 그 탄의 몬스터가 전부 죽거나 닿으면 끝난다.
5. **상점**에서 골드로 능력치·**무기(장착 3칸)**·**아이템(전투 중 사용)**·패시브를 사고,
   **「영웅」 탭에서 안뜰 여섯 자리를 다시 짠다**(중간 정비).
6. 40탄까지. 크리스탈이 0 이 되면 끝.

**근거리 캐릭터는 없다.** 영웅은 전부 벽 너머로 쏜다 — 사거리가 안쪽 길(217px)에 닿지 않으면
그 영웅은 아무것도 못 한다.

**풀하우스 이상**(풀하우스 · 포카드 · 스트레이트 플러시 · 로열)이 나오면 연출이 화려해진다 —
섬광 · 빛살 · 고리 · 카드가 깨져 흩어짐 · 화면 흔들림. 등급이 높을수록 전부 세진다.

갈무리 절차는 [`WRAPUP.md`](WRAPUP.md). 전체 설명은 [`README.md`](README.md).

---

## 뼈대

```
core/
  poker.gd     ★순수. 카드 52장과 족보 판정. 화면도 저장도 모른다.
  balance.gd   ★순수. 게임의 숫자가 전부 여기 있다 (투기장·등급·상점·탄 편성).
  roster.gd    ★자동 생성. 캐릭터 31명 · 몬스터 14종 표. 손으로 고치지 마라.
  look.gd      색 · 글꼴 · 카드와 무늬 그리기
  art.gd       그림 캐시. 그림이 없으면 색 도형으로 대신 그린다.
  ui.gd        그리면서 동시에 누를 자리를 등록하는 아주 작은 버튼 도우미
  save.gd      (오토로드 Save) 저장 파일 하나
  run.gd       (오토로드 Run) 한 판의 상태 전부 — 목숨·골드·영웅·카드·상점
game/
  main.gd          화면 갈아 끼우기. 게임의 진행 순서가 여기 한 줄로 보인다.
  title_screen.gd  타이틀
  draw_screen.gd   카드 다섯 장 고르기 + 족보 확정 연출 ★화려한 곳 (+ 자리가 없을 때 교체 창)
  hero_view.gd     ★영웅 편성 판. 안뜰 여섯 + 인벤토리. **뽑기 화면과 상점이 같은 것을 쓴다**
  battle_sim.gd    ★전투 계산. **그리기를 전혀 모른다.** 몬스터는 Balance.path_at() 위를 걷는다.
  battle_screen.gd 전투 그리기 (BattleSim 을 하나 들고 그린다)
  shop_screen.gd   상점
  over_screen.gd   끝
  fx.gd            이펙트 (뒤 layer / 앞 layer 로 나뉜다)
tools/
  roster.json      ★캐릭터·몬스터의 원본. 여기만 고친다.
  gen_art.py       로컬 Krea 2 Turbo 로 도트 그림 53장
  gen_roster.py    roster.json → core/roster.gd
  screenshot.py    Xvfb 로 실제 화면 촬영
  check_font.py    폰트에 없는 글자 찾기
  verify.sh        검증 한 곳
tests/
  poker_check      족보 전수 검사 (2,598,960판)
  ns_check         표끼리 어긋나지 않는가 (이름·등급·사거리·투기장·그림)
  play_check       화면을 실제로 눌러서 한 바퀴
  balance_check    자동 플레이로 40탄까지 (policy.gd 가 "기준 실력")
  shot             촬영 전용 진입점 (★커맨드라인을 읽는 곳은 여기뿐)
```

---

## 작업을 마칠 때 (매번, 빠짐없이)

```bash
tools/verify.sh          # 전부 (약 3분)
tools/verify.sh quick    # 빠르게 (약 1분)
```

그리고 **화면을 고쳤으면 사진을 새로 찍고 눈으로 봐라**, **APK 를 새로 구워라.**
★ APK 는 **언제나 홈 디렉터리에 굽는다** — `~/pokerdefense-test.apk`. 저장소 안(`build/`)이
아니다. 다 구웠으면 그 경로를 답변에 적어라. 자세한 것은 [`WRAPUP.md`](WRAPUP.md).

---

## 절대 깨면 안 되는 것

### 판정과 표

1. **`core/poker.gd` 는 순수 함수만 둔다.** 화면·저장·난수를 넣지 마라.
   전수 검사(2,598,960판)가 이 파일 하나에 기대고 있다.
2. **A 는 스트레이트의 양쪽 끝에 다 선다.** A-2-3-4-5(휠)와 10-J-Q-K-A 둘 다 스트레이트다.
   이걸 빼먹는 것이 족보 판정에서 제일 흔한 실수다.
3. **`Poker.Hand` 의 숫자를 바꾸지 마라.** 그 값이 곧 캐릭터 등급 인덱스이고
   저장 파일에도 그 숫자가 들어간다.
4. **`core/roster.gd` 는 자동 생성이다.** 원본은 `tools/roster.json`.
   `verify.sh` 1번이 둘이 어긋나면 잡는다.
4-1. **그림 크기 보정(`sc`)은 `roster.json` 에만 적는다.** 그림은 등급마다 같은 높이로
   저장되는데 그 높이는 지팡이·꼬리·회오리까지 포함한 **테두리 상자**의 높이라,
   소품이 큰 캐릭터는 사람 몸이 작게 나온다. `sc` 가 그 몫을 되돌린다(0.86~1.16).
   ★ **영웅을 그릴 때는 반드시 `Art.draw_unit()`** 을 쓴다. `draw_actor()` 를 직접 부르면
   보정이 빠져서 화면 세 곳(전투·확정 연출·편성 판)의 크기가 서로 달라진다.
5. **캐릭터 id 는 게임 전체에서 유일해야 한다.** 겹치면 그림 파일이 조용히 덮어써진다.
   실제로 `bolt_ranger` 가 투페어와 트리플에 둘 다 있었다 (`ns_check` 가 잡는다).

### 그리기

6. **`♠ ♦ ♣` 를 글자로 그리지 마라.** 번들 폰트에 없다. `Look.draw_suit()` 이 도형으로 그린다.
   `×`(U+00D7) 도 없다 — "2배" 처럼 한글로 써라. (`tools/check_font.py`)
7. **버튼은 그리면서 동시에 `Ui` 에 등록한다.** 그리기와 누를 자리를 따로 적으면
   "버튼은 옮겼는데 눌리는 자리는 그대로"인 버그가 반드시 생긴다.
8. **빛살·고리는 `Fx.draw_back()`, 나머지는 `Fx.draw()`.** 전부 앞에 그리면
   로열 연출에서 빛살이 족보 이름을 덮어 무슨 족보인지 안 보인다. 실제로 그랬다.
9. **연출은 언제든 탭으로 넘길 수 있어야 한다.** 40탄을 도는 게임에서 못 넘기는 연출은 고문이다.
10. **도형이 0 으로 줄어들 때 `draw_colored_polygon` 을 부르지 마라.** 점이 한 곳에 뭉쳐
    `triangulation failed` 오류가 프레임마다 쏟아진다. 안 보일 만큼 작으면 건너뛴다.
10-1. **체력 막대는 배우를 다 그린 뒤 한 번에 그린다**(`_draw_hp_bars`). 몬스터와 같이
    그리면 그리는 차례가 y 순서라 아래쪽 몬스터가 위쪽 몬스터의 막대를 덮는다.
    그리고 **바깥 길 맨 위에 선 몬스터는 막대가 위쪽 정보띠(y<68)에 가려지므로**
    y 를 72 아래로는 안 내린다.
10-2. **몬스터에 덧그리는 것(얼음·화상)은 스프라이트를 가리지 않는 자리에 둔다.**
    얼음 기둥을 몸 한가운데에 세웠더니 스프라이트에 통째로 가려져 한 조각도 안 보였고,
    앞에 다 그렸더니 이번에는 얼굴이 지워졌다. 지금은 **발밑 고리** 위에 세우고
    뒤쪽은 스프라이트 앞에, 앞쪽은 뒤에 — 다리만 가린다.

### 영웅 편성

14-3. **영웅을 넣고 빼는 것은 `Run` 의 함수들(`gain_hero` · `swap_field_bench` ·
    `bench_to_field` · `swap_field`)로만 한다.** 화면에서 `heroes`/`bench` 배열을 직접
    자르면 **겹친 수(n)가 딸려 오지 않거나** 같은 캐릭터가 두 자리에 생긴다.
    `ns_check._check_roster_slots()` 가 마흔 번 받아 보고 그걸 잡는다.
14-4. **인벤토리(25칸)는 절대 넘치지 않는다.** 캐릭터가 31명이고 안뜰이 6칸이라
    31 - 6 = 25 다. 넘칠 수 있게 두면 "내 영웅이 조용히 사라졌다"는 규칙을 만들게 된다.
14-5. **겹치기는 공격력만 곱한다**(`Balance.stack_atk`). 공격속도·사거리를 같이 올리면
    같은 영웅 n명을 나란히 세웠던 예전과 셈이 달라져서 밸런스를 다시 잡아야 한다.

### 전투와 숫자

11. **`BattleSim` 은 그리기를 모른다.** 이 머신에는 화면이 없어서, 시뮬레이터가 따로 있어야
    헤드리스로 40탄을 수백 번 돌려 볼 수 있다. 화면에 붙일 것은 `events` 배열로 흘린다.
12. **몬스터 좌표는 한 걸음에 한 번만 계산한다**(`_cache_positions`). 사거리를 잴 때마다
    cos/sin 을 다시 돌리면 영웅 40 × 몬스터 60 = 한 프레임에 2,400번이고,
    자동 플레이 검사가 몇 분씩 걸린다.
13. **몬스터가 죽어 배열이 당겨지면 탄이 들고 있는 목표 번호도 같이 고쳐야 한다**(`_reap`).
    안 그러면 탄이 엉뚱한 놈을 쫓아가고 마지막 한 마리가 안 잡힌다.
14. **가장 짧은 사거리는 안쪽 길의 절반은 덮어야 한다.** 영웅은 안뜰(반지름 92~150)에
    서고 안쪽 길은 반지름 217 이다. 못 덮는 영웅은 벽만 보고 서 있게 된다 —
    없앤 「수호」가 정확히 그랬다. `ns_check` 의 `_check_geometry` 가 이걸 지킨다.
14-1. **문에서 문까지 `LANE_SWEEP` 만큼 정확히 돌아야 한다.** `GATE_A` 를 고치면
    `LANE_SWEEP` 도 같이 고쳐라. 어긋나면 몬스터가 벽을 통과해 걸어간다.
14-2. **목숨은 `BattleSim._reap()` 한 곳에서만 깎는다.** 화면이 결과창에서 또 깎으면
    두 배로 깎인다(예전 「시간이 끝나면 남은 수만큼」 규칙의 흔적이다).
15. **둔화는 곱하지 말고 센 쪽으로 덮어쓴다.** 곱하면 둘만 겹쳐도 몬스터가 멈춘다.
15-1. **`frost` 사건은 「처음 얼어붙는 순간」에만 흘린다.** 둔화는 명중마다 다시 걸린다 —
    서리 부적·「서리 심」은 모든 명중에, 장판은 0.25초마다 붙인다. 매번 흘리면 초당
    수백 개가 쌓여 이펙트가 화면을 덮는다.
16-0. **앞 탄은 `Balance.early_tough()` 가 따로 두껍게 한다.** 예전에는 1탄 떼거리가
    3.2 라 **어떤 영웅이든 한 방**이었다 — 막대도 못 보여 주고 그 탄이 8초에 끝나서
    (길을 걷는 데 29초다) 게임을 켠 사람이 처음 보는 장면이 그것이었다. 지금은 1탄이
    3.3배라 보통 영웅이 **세 방쯤** 때려야 죽는다. ★ **곡선의 지수(1.469)는 건드리지
    마라** — 덤은 8탄쯤이면 사라지므로 뒤 탄과 클리어율이 그대로 남는다.
16. **밸런스 숫자를 감으로 고치지 마라.** `tests/balance_check` 를 돌려라. 기대값은
    **클리어율 약 25~30%** (`--runs 12` 면 2~5판, `--runs 24` 면 5~9판), **1~6탄은 크리스탈을
    잃지 않음**. 도달 탄 중간값은 12판 표본에서 크게 흔들린다(26~33탄) — 클리어율을 봐라.
    ★ 안뜰을 여섯 자리로 묶은 뒤에도 클리어율은 그대로였다(24판 6판 = 25%). 자리를 뺏긴
    만큼을 **겹치기가 돌려준다** — 같은 캐릭터를 또 뽑아도 한 톨도 안 버려지기 때문이다.
    (자리 제한만 풀어 본 A/B: 24판 중 4판. 차이는 표본 흔들림 안이다)
    ★ 중간값이 예전(35~39탄)보다 낮은 것은 규칙이 바뀌었기 때문이다. 크리스탈은 몬스터가
    닿는 즉시 깨지므로, 한 탄을 못 막으면 그 탄에서 스무 개가 통째로 날아간다. 그래서
    "언제 죽는가"가 곧 "어느 탄에서 막지 못했는가"이고, 중간값은 그 벽의 위치다.
    **골드는 지렛대가 가장 세다** — `kill_gold` 를 25% 올렸더니 클리어가 4/24 → 21/24 이 됐다.
    체력 곡선을 먼저 만지고 골드는 마지막에 만져라.
17. **`Run.total_dps()` 는 안뜰에 선 영웅만, 단일 대상 기준이다.** 인벤토리는 안 센다. 광역·연쇄·장판이 여럿을 동시에 때리는
    몫은 안 들어 있다 — 몬스터가 50마리면 실제 피해가 여덟 배까지 나온다. 어림수로만 써라.
    ★ 보스가 크리스탈 다섯 개를 부수는 이유가 이것이다. 보스는 혼자라 광역의 몫을 못 받아서,
    보스가 하나만 부수면 "떼거리만 잡고 보스는 통과시키는" 것이 최선의 수가 된다.
18. **무기 효과는 `Run.wpn_mult/wpn_add/wpn_best` 를 통해서만 읽는다.** 상점 설명과 전투가
    같은 함수를 봐야 "상점에는 +18% 라고 적혀 있는데 실제로는 안 오르는" 일이 안 생긴다.

### 도구

19. **커맨드라인은 `tests/` 만 읽는다.** 게임 코드가 인자를 읽으면 촬영 도구와 검사기가
    서로의 인자를 삼키고 `get_tree().quit()` 이 촬영 도중 앱을 죽인다.
20. **검사에는 반드시 `timeout` 과 `stdbuf -oL`.** 멈춤은 조용한 대기가 아니라 실패다.
21. **`POCKER_NO_SAVE=1`** 로 검사를 돌린다. 안 그러면 검사가 만든 값이 "내 기록"이 된다.

---

## 자주 쓰는 명령

```bash
~/.local/bin/godot -e --path .                                   # 편집기
~/.local/bin/godot --headless --path . --import                  # 임포트만 (새 PNG 를 넣은 뒤)
~/.local/bin/godot --headless --path . --quit-after 200          # 부팅 확인

tools/verify.sh                                                  # 검증 전부
tools/verify.sh quick

POCKER_NO_SAVE=1 stdbuf -oL ~/.local/bin/godot --headless --path . res://tests/poker_check.tscn
POCKER_NO_SAVE=1 stdbuf -oL ~/.local/bin/godot --headless --path . res://tests/ns_check.tscn -- --strict
POCKER_NO_SAVE=1 stdbuf -oL ~/.local/bin/godot --headless --path . res://tests/play_check.tscn
POCKER_NO_SAVE=1 stdbuf -oL ~/.local/bin/godot --headless --path . res://tests/balance_check.tscn -- --runs 12 --verbose

python3 tools/screenshot.py                       # 기본 열세 장
python3 tools/screenshot.py reveal:9 battle:14    # 로열 연출 · 14탄 전투
python3 tools/screenshot.py frost:12 battle:25    # 얼음(둔화) 연출 · 보스탄(보스 체력 띠)
python3 tools/screenshot.py shopw:9 shopi:9       # 상점의 무기 탭 · 아이템 탭
python3 tools/screenshot.py swap:14 shoph:14      # 영웅 교체 창 · 상점의 영웅 탭
python3 tools/gen_art.py --list                   # 그림 53장 목록
python3 tools/gen_art.py --only 용기사 --force
python3 tools/gen_roster.py                       # roster.json → core/roster.gd
python3 tools/check_font.py

# ★ APK 는 언제나 홈 디렉터리에. 저장소 안에 굽지 마라.
~/.local/bin/godot --headless --path . \
    --export-debug "Android Test APK" "$HOME/pokerdefense-test.apk"
```

---

## 이 머신(aarch64, 화면 없음)에서 알아둘 것

- **화면이 없다.** `tools/screenshot.py` 가 Xvfb 를 띄워 소프트웨어 OpenGL 로 실제로 그린다.
  느리다 — 전투 한 장에 1~2분. 여러 장을 한 번에 넘기면 Godot 을 한 번만 띄운다.
- **그림은 로컬 Krea 2 Turbo(`/home/dgxmaruta/pjt/krea2`)로 만든다.** 모델 올리는 데 3~4분,
  한 장에 65초. 53장이면 약 57분이다.
- **APK 는 홈 디렉터리(`~/pokerdefense-test.apk`)에 굽는다.** 저장소 안에 두지 마라 —
  `tools/verify.sh` 9단계도 그리로 굽는다.
- **APK 는 `use_gradle_build=false` 라서 만들어진다.** x86_64 전용인 `aapt2` 를 안 탄다.
  스토어용 **AAB 는 이 머신에서 안 된다.** iOS 는 Xcode 프로젝트까지만.
- **`import_etc2_astc=true` 를 지우지 마라.** 없으면 x86_64 러너에서 오류 메시지도 없이
  익스포트가 거부된다.
- `pkill -f "..."` 는 **그 문자열이 지금 셸의 명령줄에도 있으면 셸 자신을 죽인다.**
  PID 를 먼저 찾아 `kill` 하라.

---

## 코드 스타일

- 들여쓰기는 **탭**. 주석과 사용자 문구는 **한국어**.
- `class_name` 은 PascalCase, 파일명은 snake_case.
- 공개 함수에는 `##` 문서 주석. **왜** 그렇게 했는지가 중요한 곳에는 근거를 남겨라 —
  특히 나중에 "최적화"하다가 되돌리기 쉬운 것들(위의 8·12·13·15번이 전부 그런 것이다).
